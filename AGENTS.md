## Notifications

To notify or inform of an action, use the `SLACK_POST_URL` environment variable
and send a POST request with the info.

## Git

Use the `/commit` skill for all commits. After committing, open a PR against
`main` — the auto-merge workflow will handle the rest.
